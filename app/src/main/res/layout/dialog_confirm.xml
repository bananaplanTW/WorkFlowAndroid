<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/confirm_dialog_width"
    android:layout_height="@dimen/confirm_dialog_height"
    android:gravity="center"
    android:background="@color/confirm_dialog_background_color"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/confirm_dialog_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/confirm_dialog_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/confirm_dialog_text_padding_top_bottom"
        android:paddingBottom="@dimen/confirm_dialog_text_padding_top_bottom"
        android:textSize="@dimen/confirm_dialog_text_text_size"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/confirm_dialog_ok_button"
        style="@style/ConfirmDialogButtonStyle.OK" />

</LinearLayout>