<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/detailed_worker_task_schedule_item_height"
    android:paddingLeft="@dimen/detailed_worker_task_schedule_item_id_width">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/detail_worker_listview_horizontal_divider">

        <TextView
            android:id="@+id/divider"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:background="@android:color/white"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            android:text="@string/detailed_worker_task_schedule_listview_divider_text"
            android:textColor="@color/gray1"
            android:textSize="14dp" />
    </RelativeLayout>
</RelativeLayout>